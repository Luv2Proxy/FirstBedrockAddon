# Extract and lift the chunk containing the executing player.
# Run while standing in the chunk you want to transform.
#
# The script reads the executing player's position and performs the operation.
scriptevent firstbedrockaddon:make_island
